metrics:
  - type: roc_auc_binary
    reducers: [mean, std]

  - type: roc_auc_macro
    reducers: [mean]

  - type: roc_auc_class
    class_name: cat
    reducers: [mean, min, max]

plots:
  - roc_auc_binary
  - roc_auc_macro
